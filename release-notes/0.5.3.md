- Notes list now tiles correctly on real devices. The previous fix worked in the simulator but not on iOS 26 hardware — switched off LazyVGrid entirely and pair notes manually two per row. Should be no more empty slots next to the first card.
- Same preview polish from 0.5.2: stripped markdown markers, ☐ / ☑ checkbox glyphs, collapsed blank-line runs.

Things to confirm:
- Open the list and scroll through. Every row should have two cards (with the last row maybe having a single card on the left).
- Add a brand-new note and confirm it still places correctly in the grid.
