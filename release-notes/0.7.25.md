- Fixed editor toolbar controls (back, share, more) being nearly invisible on dark sticky colors — they now use a contrasting per-color tint.
- Fixed list rows showing an empty snippet for single-long-line stickies; the row now continues the body text past the title.
- Fixed the capture screen's close button not aligning with the timer text — the icon and timer now share a baseline.
