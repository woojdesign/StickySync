- **Cleaner notes list.** The grid no longer leaves empty cells between rows — every slot is filled left-to-right.
- **Tidier card previews.** Headings, bullets, and checkboxes render as clean text + ☐ / ☑ glyphs on the cards. Multiple blank lines between heading and body are collapsed so cards are tighter.

Things to confirm:
- Open the list and scroll through your notes. Cards should tile evenly two-up with no gaps.
- A note that starts with `# heading` and has blank lines below should look clean on its card.
- Cards with checkboxes (`- [ ]` / `- [x]`) should show ☐ / ☑ glyphs in the preview, not raw `- [ ] ` text.
