- Added a sharing UI exposing all four permission options.
- Restored the "Share text… / Share with someone…" menu on the share button.
- Added the ability to share a note with someone, with creation handled through the system share picker.
- Added rendered Markdown, with bold, italic, and other inline syntax displayed as styled text instead of raw markers.
- Hid Markdown markers, collapsing their width and revealing them only when the cursor is on the line.
- Added rendered checkboxes (☐ / ☑) that can be toggled by clicking them.
- Added automatic list continuation, list paragraph indentation, and `[]` checkbox autoexpansion.
